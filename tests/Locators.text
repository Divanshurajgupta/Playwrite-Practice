1. Page.getByRole()| to locate by explicit and iplicit accessibility attributes.
2. page.getByText()|to locateby text content.
3. page.getByLabel()| to locatea form control by associated label's text
4. page.getByplaceholder()| to locate an input by placeholder.
5. page.getByAltText()| to locate an element usually image by its text alternative.
6. page.getByTitle()| to locate an element by its tilte attributes
7. page.getByTestId()| to locate an element based on its data-testid attribute(other attributes can be configured)
